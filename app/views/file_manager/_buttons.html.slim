.action-buttons
  = t('.files')
  - %w(copy cut paste itemSettings lock unlock downloadItem delete play toPlaylist).each do |button|
    a href="" class=['action-button', button] ng-click="#{button}()" title=t(".#{button}")
